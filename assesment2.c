#include<stdio.h>
int main ()
{
    int x,y;
    printf("Enter Number : ");
    scanf("%d",&x);
    
   /*1.Get a number from the user and check if the number equals 50. If yes, the print 1, otherwise print 0 
   printf("%d",x==50);*/

   /*2.Get a number from the user and check if the number equals 50. If yes, the print 0, otherwise print 1
   printf("%d",x!=50);*/

   /*3.Get a number from the user and check if the number is less than 50. If yes, then print 1; otherwise, print 0 
   printf("%d",x<50);*/\

   /*4.Get a number from the user and check if the number is less than 50. If yes, then print 1; otherwise, print 0 
   printf("%d",x>50);*/

   /*5.Get a number from the user and check if the number is less than or equal to 50. If yes, then print 1; otherwise, print 0
   printf("%d",x<=50);*/

   /*6.Get a number from the user and check if the number is greater than or equal to 50. If yes, then print 1; otherwise, print 0
   printf("%d",x>=50);*/

   /*7.Get a two-digit number from the user and check if the digit 0 and digit 1 are identical. If yes, print 1; otherwise, print 0.
   y = x/10;
   x=x%10;
   printf("%d",x==y);*/

   /*8.Get a two-digit number from the user and check if the digit 0 and digit 1 are identical. If yes, print 0; otherwise, print 1.
   y = x/10;
   x=x%10;
   printf("%d",x!=y);*/

   /*9.Get a two-digit number from the user and check if the digit 0 is less than the digit 1. If yes, print 1; otherwise, print 0
   y = x/10;
   x=x%10;
   printf("%d",x<y);*/

   /*10.Get a two-digit number from the user and check if the digit 0 is greater than the digit 1. If yes, print 1; otherwise, print 0 
   y = x/10;
   x=x%10;
   printf("%d",x>y);*/

   /*11.Get a two-digit number from the user and check if the digit 1 is less than or equal to the digit 0. If yes, print 1; otherwise, print 0.
   y = x/10;
   x=x%10;
   printf("%d",x>=y);*/

   /*12.Get a two-digit number from the user and check if the digit 1 is greater than or equal to the digit 0. If yes, print 1; otherwise, print 0.
   y = x%10;
   x=x/10;
   printf("%d",x>=y);*/

   /*13.Get a four digit number form the user if digit 1 and digit 0 is same then return 1 else 0
   y = x%10;
   x=x/10;
   x=x%10;
   printf("%d",x==y);*/
   /*14.Get a four-digit number from the user and check if the first 2 digits and last 2 digits are the same. If yes, print 1; otherwise, print 0.
   y =  x%100;
   x=x/100;
   printf("%d",x==y);*/
}